frontend:
  image: "{{ registry.url }}/{{ registry.namespace }}/ci-cd-gcp-chat-web-application-frontend"
  tag: "{{ frontend_tag }}"
  port: 3000
  nginx_exporter_port: 9104
  env:
    REACT_APP_LOCALHOST_KEY: "chat-app-current-user"
